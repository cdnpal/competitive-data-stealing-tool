package com.carsdirect.cd.service;

public class ThreadPoolExecutorServiceImpl implements ThreadPoolExecutorService { 
	
	/*
	 * Eventually a 10 thread thread pool was set up to keep the network
	 * traffic to the CarsDirect competitor believable and the Thread.sleep()
	 * time between requests was also randomized to avoid detection.
	 */

}
